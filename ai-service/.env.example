# AI Service environment – LOCAL development
# No external keys needed for mock mode.
# Replace these when wiring real OpenAI / embedding models.
OPENAI_API_KEY=sk-replace-me
MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
PORT=8000
